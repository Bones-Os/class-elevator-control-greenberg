# class-elevator-control-greenberg

Title: proj1

Project goal: The goal of this project is to gain experience using linked lists

Important files: centcom.cpp, centcom.h, centcom.o, driver.cpp, driver.o, makefile, mytest.cpp, mytest.o

A C++ program which simulates an elevator control via linked lists. Upon running, it goes through a set sequence of events.

Note that this was an academic assignment. That means that the function headers as well as what program should do were given by the professor.
